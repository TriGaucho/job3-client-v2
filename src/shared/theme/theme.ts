import { createTheme } from '@mui/material/styles';

const theme = createTheme({
    palette: {
        primary: {
            main: 'rgb(242, 172, 38)',
        },
        background: {
            default: '#eee',
        },
    },
});

export default theme;
